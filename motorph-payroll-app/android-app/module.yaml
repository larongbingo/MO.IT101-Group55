product: android/app

dependencies:
  - ../shared
  - androidx.activity:activity-compose:1.7.2

settings:
  compose: enabled
  junit: junit-4
  jvm:
    jdk:
      version: 25
      selectionMode: alwaysProvision